== LiveMap

LiveMap is a Rails application that displays geographic locations on a heatmap using 
link:http://www.patrick-wied.at/static/heatmapjs/.

Geographic locations can be added via a POST request containing a street address.
Heatmaps are updated client-side via link:http://pusher.com/.
